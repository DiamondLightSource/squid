

import React from 'react'

// numbered stages and a persistent state across them. can increment and decrement
// should also have stages context with dispatch to interact with

// stage names: choose element, calibrate detectors, calivrate scan parameters, collect your data

function Stages() {
    return (
        <div>Stages</div>
    )
}

export default Stages
